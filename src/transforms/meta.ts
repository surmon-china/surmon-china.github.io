export function getMetaTitle(ID: string): string {
  return `${ID} | Homepage`
}

export function getMetaDescription(ID: string): string {
  return `${ID} GitHub homepage`
}

export function getMetaKeywords(ID: string): string[] {
  return [`${ID} examples`, `How to use ${ID}`]
}
